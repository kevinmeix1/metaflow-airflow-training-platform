apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: demand-training-spot
spec:
  template:
    metadata:
      labels:
        workload-tier: training
    spec:
      nodeClassRef:
        group: karpenter.k8s.aws
        kind: EC2NodeClass
        name: training-default
      requirements:
        - key: karpenter.sh/capacity-type
          operator: In
          values: ["spot", "on-demand"]
        - key: kubernetes.io/arch
          operator: In
          values: ["amd64"]
  limits:
    cpu: "192"
    memory: 768Gi
  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 5m
---
apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: training-default
spec:
  role: eks-auto-mode-node-role
  subnetSelectorTerms:
    - tags:
        kubernetes.io/role/internal-elb: "1"
  securityGroupSelectorTerms:
    - tags:
        app.kubernetes.io/part-of: metaflow-airflow-training-platform
  tags:
    cost-center: training-platform
    workload: partitioned-backfill
