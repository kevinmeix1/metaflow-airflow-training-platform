name: Training Orchestration CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  demo-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
      attestations: write
      artifact-metadata: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Static Python compile
        run: PYTHONPATH=src python3 -m compileall -q src tests
      - name: Run deterministic demo
        run: make demo
      - name: Validate generated artifacts
        run: make ci-verify
      - name: Run tests
        run: make test
      - name: Build CI summary
        if: always()
        run: |
          {
            echo "### Training orchestration demo artifacts"
            echo ""
            echo "- Dashboard: .local/reports/training_orchestration_dashboard.html"
            echo "- Governance evidence: .local/reports/governance_evidence_bundle.json"
            echo "- SLO report: .local/reports/slo_error_budget.json"
            echo "- Cloud migration plan: .local/reports/cloud_migration_plan.json"
            echo "- Supply-chain evidence: .local/reports/supply_chain_evidence.json"
            echo "- Orchestration scorecard: .local/reports/orchestration_scorecard.json"
            echo "- Accelerator capacity plan: .local/reports/accelerator_capacity_plan.json"
            echo "- Device allocation plan: .local/reports/device_allocation_plan.json"
            echo "- DRA resource health status: .local/reports/resource_health_status_plan.json"
            echo "- Advanced device sharing: .local/reports/advanced_device_sharing_plan.json"
            echo "- AdminAccess diagnostics: .local/reports/admin_access_diagnostics_plan.json"
            echo "- In-place resize plan: .local/reports/inplace_resize_plan.json"
            echo "- Topology placement plan: .local/reports/topology_placement_plan.json"
            echo "- KubeRay capacity plan: .local/reports/kuberay_capacity_plan.json"
            echo "- Inference gateway plan: .local/reports/inference_gateway_plan.json"
            echo "- Semantic telemetry plan: .local/reports/semantic_telemetry_plan.json"
            echo "- Deadline alert plan: .local/reports/deadline_alert_plan.json"
            echo "- Cost observability: .local/reports/cost_observability_report.json"
            echo "- Elastic workload plan: .local/reports/elastic_workload_plan.json"
            echo "- Indexed job resilience: .local/reports/indexed_job_resilience_plan.json"
            echo "- Provisioning admission: .local/reports/provisioning_admission_plan.json"
            echo "- MultiKueue dispatch: .local/reports/multikueue_dispatch_plan.json"
            echo "- DAG bundle versioning: .local/reports/dag_bundle_versioning_plan.json"
            echo "- Asset partitioning: .local/reports/asset_partitioning_plan.json"
            echo "- Airflow 3.3 stateful orchestration: .local/reports/airflow_stateful_orchestration_plan.json"
            echo "- Multi-team readiness: .local/reports/multi_team_readiness_plan.json"
            echo "- Event-driven assets: .local/reports/event_driven_assets_plan.json"
            echo "- Pod resource envelopes: .local/reports/pod_resource_envelope_plan.json"
            echo "- Cohort fair sharing: .local/reports/cohort_fair_sharing_plan.json"
            echo "- Flavor fungibility: .local/reports/flavor_fungibility_plan.json"
            echo "- Pending workload visibility: .local/reports/pending_workload_visibility_plan.json"
            echo "- Tenant fairness: .local/reports/tenancy_fairness_report.json"
            echo "- Workload identity: .local/reports/identity_access_report.json"
            echo "- Performance budget: .local/reports/performance_budget.json"
            echo "- Queue simulation: .local/reports/queue_simulation.json"
            echo "- Workload-aware scheduling: .local/reports/workload_aware_scheduling_plan.json"
            echo "- Runtime security: .local/reports/runtime_security_plan.json"
            echo "- Control-plane diagnostics: .local/reports/control_plane_diagnostics_plan.json"
            echo "- Memory QoS: .local/reports/memory_qos_plan.json"
            echo "- HPA scale to zero: .local/reports/hpa_scale_to_zero_plan.json"
            echo "- Suspended Job resources: .local/reports/suspended_job_resources_plan.json"
            echo "- Constrained impersonation: .local/reports/constrained_impersonation_plan.json"
            echo "- OCI artifact volumes: .local/reports/oci_artifact_volume_plan.json"
            echo "- Release admission: .local/reports/release_admission_decision.json"
            echo ""
            echo "Generated reports:"
            find .local/reports -maxdepth 1 -type f | sort | sed 's/^/- /'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload demo artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: training-orchestration-demo-artifacts
          path: |
            .local/reports
            .local/governance
            .local/orchestration
            .local/data/manifests
            .local/mlruns
          if-no-files-found: error
      - name: Attest demo report provenance
        if: github.event_name == 'push'
        uses: actions/attest@v4
        with:
          subject-path: ${{ github.workspace }}/.local/reports/**

  metaflow-runtime-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-metaflow.lock
      - name: Install the constrained Metaflow runtime
        run: python -m pip install --constraint requirements-metaflow.lock --editable ".[metaflow-runtime,dev]"
      - name: Verify dependency consistency
        run: python -m pip check
      - name: Lint Python contracts
        run: ruff check .
      - name: Execute and verify the Metaflow contract
        run: make metaflow-runtime-contract
      - name: Publish runtime summary
        if: always()
        run: |
          python - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json
          from pathlib import Path

          path = Path(".local/metaflow/verification.json")
          print("### Executable Metaflow contract")
          print()
          if not path.exists():
              print("Runtime verification did not produce evidence.")
              raise SystemExit(0)
          result = json.loads(path.read_text())
          print(f"- Metaflow: {result['metaflow_version']}")
          print(f"- Run: `{result['run_id']}`")
          print(f"- Graph: {result['step_count']} steps / {result['task_count']} tasks")
          print(f"- Candidate fan-out: {result['candidate_count']}")
          print(f"- Selected: `{result['selected_candidate']}`")
          print(f"- Rendered cards: {result['card_count']}")
          PY
      - name: Upload Metaflow runtime evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: executable-metaflow-runtime
          path: |
            .local/metaflow
            .local/models
            .local/mlruns/daily-demand-forecasting
            .local/reports/metaflow_*.json
            .local/reports/candidate_comparison_*.json
            .local/reports/training_orchestration_dashboard.html
          if-no-files-found: error

  airflow-33-sdk-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
      - name: Install constrained Airflow 3.3 runtime
        run: python -m pip install "apache-airflow==3.3.0" --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-3.3.0/constraints-3.11.txt"
      - name: Verify dependency consistency
        run: python -m pip check
      - name: Parse and validate Airflow 3.3 DAGs
        run: make airflow-sdk-contract
